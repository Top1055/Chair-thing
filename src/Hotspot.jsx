import React, { useEffect } from "react";

const Hotspot = ({ identifier, position, normal, content }) => {
    // Hotspot component does not render anything itself.
    // It's used to pass hotspot information to the ModelViewerComponent.
    return null;
};

export default Hotspot;
